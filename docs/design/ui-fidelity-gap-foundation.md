# UI 还原度差距清单 · 地基三屏（登录页 / 应用外壳 / 工作台首页）

状态：Draft（二审已修订）｜ 2026-06-17 起草、2026-06-18 复核更正 ｜ 作为「UI 还原收口切片」任务包的依据

> **二审更正(重要)**：初稿对 `App.tsx` 读取不全,误把外壳的面包屑/全局搜索壳/铃铛真实未读、工作台首页的
> 真实未读/通知/应用入口接线当成"缺失/占位"。已据当前代码核实更正(见 §2 S-3/S-6/S-7/S-8、§3 整段)。**真问题
> 是视觉语言未对齐设计,不是功能缺失;整改=换皮+对齐组件库,已交付的真实接线一律回归保留、勿删勿造假。**

## 这份文档是什么

把**设计真源**（`docs/design/ui-handoff/design/`，来自 Claude Design 交接包，README 要求**像素级还原**）
与**现有实现**逐项对照，列出地基三屏的不一致，供收口切片照单整改。

- 设计真源：`docs/design/ui-handoff/design/企业工作台设计规范.html`（§02 登录页、§03 应用外壳+工作台首页）
  - `tokens.css`（设计 token 唯一真源，主色 `#3370FF` 飞书蓝、4px 间距基准）。
- 现有实现：`apps/workbench-shell/src/app/App.tsx`（`LoginView` / `AppShell` / `WorkbenchHome`）
  - `apps/workbench-shell/src/styles.css` + `packages/ui/src/styles/{tokens.css,components.css}`。

## ⚠️ 一个必须先讲清的还原边界（重要）

设计稿是一个**虚构的、数据填满的企业门户**——里面画了**本产品当前并不存在的功能**：
「应用管理 / 在用应用 48 个 / 应用清单表格 / 本月工单 236 / 文档资料 / 审批中心(已建)」等。
因此"像素级还原"必须**分两层**，否则会要求实现根本没有的后端：

- **L1 视觉系统 + 外壳 chrome + 真实存在的屏 → 严格像素级还原**（token、间距、圆角、阴影、
  组件态、登录页、侧栏/顶栏布局骨架、工作台首页的版式语法）。这是本切片的硬门禁。
- **L2 映射到未建功能的内容 → 仅作视觉参考**：用设计的**视觉语言**去渲染**真实有的数据**
  （如工作台首页用设计的 stat-card / card / table 样式，但卡片内容接真实的通知/在岗/待办，
  而不是硬塞"48 个应用""本月工单 236"这些没有数据源的虚构内容）。功能屏(组织成员/消息中心/
  审批)按各自里程碑落地时,照对应设计稿做 L1 还原。

> 也就是说:**组件库与外壳要像素级对齐设计;工作台首页要用设计的组件重新搭建版式,但内容接真实数据**。
> 收口切片不负责把"应用管理/工单"这些虚构功能做出来。

## 1. 登录页（设计 §02 vs `LoginView`）

| #   | 项                                        | 设计真源                                                                   | 现有实现                                  | 判定                                            |
| --- | ----------------------------------------- | -------------------------------------------------------------------------- | ----------------------------------------- | ----------------------------------------------- |
| L-1 | Logo 字符                                 | `工`（品牌字 mark）                                                        | `W`                                       | ❌ 字符错                                       |
| L-2 | 副标题文案                                | 企业内网账号统一登录入口                                                   | 企业内部协作与管理入口                    | ❌ 文案不符                                     |
| L-3 | 输入框前缀图标                            | 内联**线性 SVG**（用户/锁）                                                | emoji `👤` `🔒`                           | ❌ 风格错（emoji 跨系统渲染不一致，非线性图标） |
| L-4 | 账号 placeholder                          | 请输入工号或邮箱                                                           | 未设（默认值 `admin`）                    | ❌ 缺 placeholder                               |
| L-5 | "记住登录"勾选                            | 默认**选中态** + 自绘 checkbox+对勾 SVG                                    | 默认未选中、组件默认态                    | ⚠️ 默认态 + 样式待对齐                          |
| L-6 | 主按钮文案                                | `登 录`（字间空格）                                                        | `登录`                                    | ⚠️ 字距不符                                     |
| L-7 | 底部提示文案                              | 登录即代表同意《内网使用规范》与《安全协议》                               | 登录即表示你正在访问企业内网工作台。      | ❌ 文案不符                                     |
| L-8 | 卡片样式（圆角/阴影/内距/宽度）+ 舞台背景 | `login-stage` 居中 + `login-card`（需逐值核对 `--r-lg`/`--shadow-pop` 等） | `.login-page`/`.login-card`（需逐值核对） | ⚠️ 待逐值比对                                   |

## 2. 应用外壳（设计 §03 vs `AppShell`）

| #   | 项               | 设计真源                                          | 现有实现                                                                                | 判定                                                                         |
| --- | ---------------- | ------------------------------------------------- | --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| S-0 | 侧栏宽度         | 220px                                             | `--shell-sidebar-width: 220px`                                                          | ✅ 骨架一致                                                                  |
| S-1 | 品牌 mark + 名称 | `工` + **内网工作台**                             | `W` + **Work Platform**                                                                 | ❌ mark 与产品名都错（英文 vs 中文）                                         |
| S-2 | 导航/顶栏图标    | 每项内联**线性 SVG**（宫格/铃铛/对勾/搜索/折叠…） | 全是 emoji/字符占位：`ModuleIcon` 取首字母、`☰`/`⌕`/`🔔`（`App.tsx` L344/357/372/673） | ❌ 图标体系错（emoji/首字母 ≠ 线性 SVG），真差距                             |
| S-3 | 侧栏导航徽标     | 消息中心 nav 项带 `badge 5`                       | 侧栏 nav 项渲染**空** `badge-slot`（L228）；**注**：顶栏铃铛徽标已接真实未读，勿与此混  | ⚠️ 仅**侧栏 nav 徽标**未接（顶栏铃铛徽标已交付，回归保留）                   |
| S-4 | 侧栏分组         | 固定三组(顶/业务应用/系统)                        | 由 **manifest 动态**生成分组                                                            | ⚠️ 设计是固定分组、实现是动态——以"分组样式/层级"对齐为准，分组内容随真实菜单 |
| S-5 | 侧栏底部用户     | 头像`张` + 姓名`张伟` + **角色**`平台运营组`      | 头像 + 姓名 + **部门**（L239）                                                          | ⚠️ 第二行设计是角色、实现是部门                                              |
| S-6 | 顶栏面包屑       | `工作台 / 概览`（两级，末级加粗）                 | **已有**面包屑 `工作台 / <b>{activeTitle}</b>`（L347）                                  | ✅ 结构已具备（回归保留）；仅末级文案随路由,基本对齐                         |
| S-7 | 顶栏搜索框       | 居中搜索框「搜索应用、文档、成员」                | **已有**搜索壳 + ⌘K + 弹层（L350-365），placeholder「搜索应用、成员、审批… ⌘K」         | ⚠️ 壳已交付(回归保留)；仅 placeholder 文案/图标(`⌕`)与设计差,样式待对齐      |
| S-8 | 顶栏右侧         | 折叠 + 搜索 + 铃铛(带红点) + **帮助** + 头像      | 折叠 + 搜索 + 铃铛(真实未读) + 头像下拉(含退出)；**无帮助图标**，且均 emoji             | ⚠️ 功能基本齐(回归保留)；差**帮助图标**缺失 + 图标改线性 SVG                 |

## 3. 工作台首页（设计 §03 内容 vs `WorkbenchHome`）

> **重要更正(二审后核实)**:`WorkbenchHome` **已经接真实数据**——未读数(L606-613)、最新消息接真实通知
> (L647-654)、常用应用接 navigationItems(L631-646),且对未建功能**诚实占位 + EmptyState、明确不展示原型假数据**
> (L657)。所以这里的真差距**不是"没接真实数据/塞了虚构内容"**(它没塞),**而是没用设计的 `stat-card`/`tbl`/
> `card`/`quick-grid` 视觉语言**(自造 `workbench-home__*` 类)。**整改=换用设计组件样式重排版式,真实数据接线
> 与诚实占位一律回归保留、勿删勿造假。**

| #   | 项                | 设计真源(视觉语言)                      | 现有实现                                             | 真差距(L1 视觉) / 勿回退(已交付)                                                                                     |
| --- | ----------------- | --------------------------------------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| W-1 | 问候区            | 大标题 + 子句版式                       | `{问候}，{姓名}` + `部门·日期`(L592-597)             | ⚠️ 版式对齐设计;子句继续接真实数据(勿写"3 项待办"假数)                                                               |
| W-2 | 头部操作          | `[刷新] [默认]` + 主按钮                | `[刷新][新建申请（M11 待接入）]`(L600-601)           | ⚠️ 按钮用 UI-1 样式;诚实标注的"M11 待接入"保留                                                                       |
| W-3 | 数据卡片          | `stat-card` 样式(图标方块+主数字+delta) | `workbench-home__stat`:未读(真实)+3 占位(M11/vNext)  | ❌ **换 `stat-card` 视觉(L1)**;内容**维持现状**(未读=真实;待审批 M11/我的待办 vNext **保持诚实占位**,不接不存在的源) |
| W-4 | 左列卡片          | `tbl`/`card` 样式                       | `workbench-home__card`:待处理事项/常用应用(真实)     | ❌ **换 `card`/`tbl` 视觉(L1)**;**不照搬设计的"应用清单"虚构表**,沿用现有真实内容(常用应用/待处理占位)               |
| W-5 | 常用入口          | `quick-grid` 宫格                       | quickEntries(真实 navigationItems)                   | ⚠️ 换 `quick-grid` 视觉;内容(真实入口)保留                                                                           |
| W-6 | 系统动态/最新消息 | 卡片/时间线视觉                         | 最新消息接真实通知(L647-654);系统动态诚实 EmptyState | ⚠️ 换设计卡片视觉;真实通知接线 + 系统动态占位**保留**                                                                |

## 4. 结论与收口切片应做的事

- **骨架对了,功能也比初稿以为的全**:侧栏 220px、token 已 port;外壳的面包屑/全局搜索壳/铃铛真实未读、
  工作台首页的真实未读/通知/应用入口接线**都已交付**(二审更正,见 §2/§3)。真问题是**视觉语言没对齐设计**:
  品牌(`W`/Work Platform)、图标体系(全 emoji/首字母)、文案、组件态、工作台用自造类而非设计的
  `stat-card`/`tbl`/`card`/`quick-grid`。登录页是"形似神不似"(emoji 图标/文案/默认态)。
- **核心整改是视觉:对齐组件库 + 换皮**,而非重写数据接线。**已交付的真实接线/诚实占位一律回归保留、勿删勿造假。**
- **token 已 port,但 `components.css`/`apps/workbench-shell/src/styles.css` 的组件与页面样式需对齐设计规范的
  "基础/业务组件"段**——组件库是所有屏复用的根,优先做。

**「UI 还原收口切片」(M8 之前)范围据此固定:**

1. 设计真源**早已在仓库**(`docs/design/ui-handoff/`,含 294 行 README 把 6 屏列全)——本切片**不是补真源,而是补"照它实现+门禁"**。
2. **组件库对齐**:`packages/ui` 的 btn/input/card/tag/table/checkbox/avatar/badge 等照设计规范"基础/业务组件"段像素级还原(L1)。
3. **登录页**像素级还原(L-1..L-8)。
4. **应用外壳**还原:重点是**品牌(S-1)、图标改线性 SVG(S-2)、侧栏 nav 徽标(S-3)、底部角色(S-5)、帮助图标(S-8)**;
   面包屑(S-6)/搜索壳(S-7)/铃铛真实未读**已交付,回归保留 + 仅对齐样式/文案/图标**;分组内容仍随 manifest(S-4)。
5. **工作台首页**用设计组件(stat-card/card/tbl/quick-grid)**换皮重排版式**(W-1..W-6);**真实数据接线与诚实占位
   全部回归保留**,不接不存在的源(待审批 M11/在岗无聚合 API),不照搬设计的虚构内容(应用清单/工单)。
6. **立"设计还原度"门禁**:**可静态核验的硬判据**(零硬编码 hex、零 emoji、关键文案 spec 逐字断言、间距/圆角/阴影
   只引 token 变量)由实现方交付前过;**人工并排视觉抽查**由评审方做(详见任务包 §2)。纳入交付验收。
7. 功能屏(组织成员→M8、消息中心→M7 回补、我的待办/审批→M11)在各自里程碑照对应设计稿做 L1 还原,不在本切片。

> 下一步:据本清单起草收口切片任务包(逐屏断言 + L1/L2 边界 + 门禁),独立 sub-agent 二审后交 Codex 实现。
